# Day 1 – Local file with Terraform

## Steps

- Initialize Terraform with `terraform init`.
- Plan infrastructure with `terraform plan`.
- Apply configuration with `terraform apply` to create `hello.txt`.
- Message content is controlled by the `message` variable.
- Output `file_path` shows where the file is created.
